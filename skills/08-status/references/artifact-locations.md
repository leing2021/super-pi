# Artifact Locations

| Artifact | Path |
|---|---|
| Brainstorm | `docs/brainstorms/` |
| Plan | `docs/plans/` |
| Solution | `docs/solutions/` |
| Runtime | `.context/compound-engineering/` |
| Handoff-lite | `.context/compound-engineering/handoffs/` |
| Context state | `.context/compound-engineering/context-state.json` |
| Checkpoints | `.context/compound-engineering/checkpoints/` |

Use `bash` with `ls` and `find` to scan these paths.
In status-lite mode, report paths and latest filenames only; do not expand full artifact content unless user asks.
